server.port: 8080

spring:
  profiles.include: db
  jpa:
    open-in-view: false
    properties.hibernate:
        format_sql: true
        default_batch_fetch_size: 100

logging.level.org.hibernate.SQL: debug